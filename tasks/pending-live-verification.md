# 웹-우선 스프린트 — 라이브 검증 대기 목록

작성: 2026-06-22 · 대상: 읽음확인(#1)·예약공개(#2)·답장(#3)·이어쓰기(#4)

로컬 검증(타입체크 0 / lint 0 / 유닛 235 / 빌드 OK)은 **통과**. 아래는 **로컬에서 증명 불가**해
프로덕션 Supabase 또는 실기기/실흐름에서만 확정되는 항목이다. 배포 후 이 체크리스트로 검증한다.

## 배포 절차 (한 번에)
- [ ] `supabase db push` — 마이그레이션 **0017_read_receipts**, **0018_scheduled_reveal** 프로덕션 적용
- [ ] `vercel --prod` — 프론트 배포
- [ ] 배포 후 아래 라이브 검증 수행

---

## #1 읽음 확인 (0017) — 마이그레이션 적용 후 흐름 검증
> 설계 변경: 링크별(per-token) 배지는 **드롭**(listLinks 임베드가 미배포/배포윈도우에 listLinks를
> 통째로 깨뜨려 결합도/취약성 큼). 읽음 확인은 **편지 단위 "열어봤어요"** 로만 제공(견고). 발송함
> 롤업은 non-fatal(실패해도 편지 목록은 보임). 링크별 N번 배지는 라이브 검증 후 재도입 고려.
- [ ] `letter_opens` 테이블/인덱스/RLS 생성 확인
- [ ] **`record_letter_open` (anon)**: 유효 토큰 → 1행 insert / 재호출 시 `open_count` 증가·`last_opened_at` 갱신(upsert)
- [ ] revoke/만료/없는 토큰 → 조용히 no-op(에러·상태 누설 없음)
- [ ] `get_my_letter_opens` 소유자 롤업 정상 + 타인 데이터 안 보임(RLS)
- [ ] **E2E 흐름**: 수신자가 "편지 열기 ▶" → 발신자 발송함 카드 "👀 열어봤어요 · N번 · 최근시각" 반영

## #2 예약 공개 (0018) — 무중단 함수 교체 검증
- [ ] `delivery_links.reveal_at` 컬럼 추가 확인
- [ ] **issue_link 4-arg→5-arg drop/recreate 무중단**: 배포 윈도우에 구(舊) 클라이언트의 4-arg 호출이 `p_reveal_at` 기본값으로 정상 매칭되는지(링크 발급 안 깨짐)
- [ ] reveal_at 미래 링크 열기 → 뷰어 **봉인 화면** "이 시각에 열려요"(본문/암호 단계 이전 차단)
- [ ] reveal_at 경과 후 동일 링크 → 정상 열림
- [ ] reveal+password 동시 링크: reveal 우선 차단(암호 보호 사실 누설 안 함)
- [ ] LinkManager에서 예약 공개 설정 → 링크에 "🔒 …공개 예정" 배지
- [ ] `NOT_YET_REVEALED:<ISO>` 메시지의 ISO가 JS `new Date()`로 정상 파싱(로컬 유닛은 통과, 실서버 timestamptz 포맷 재확인)

## #3 답장 (백엔드 0, 순수 프론트)
- [ ] `/create?to=<userId>` → "연결된 사람에게 보내기" 탭 기본 + 그 상대 **자동 선택**(연결된 경우)
- [ ] 비연결 상대 id면 graceful(선택 안 됨, 링크 탭으로 폴백 가능)
- [ ] 받은편지함/스레드의 "답장" 버튼 네비게이션 + 실제 발송까지 1회 흐름

## #4 이어쓰기
- [ ] 발송함 "이어쓰기" → `/create/:id` 기존 편지 로드 후 편집·재저장(진입점 신규, 로직은 기존)

## 공통 / 기존 미검증(회귀 가능)
- [ ] 실기기 iOS 편지 음악 재생(헤드리스 불가 — 기존부터 실기기 한정)
- [ ] e2e 결과 반영(이번 실행분)
